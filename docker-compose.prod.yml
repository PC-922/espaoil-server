version: '3.8'

services:
  espaoilserver:
    build: 
      context: .
      dockerfile: Dockerfile.prod
    container_name: espaoilserver
    ports:
      - "8080:8080"
    networks:
      - coolify-infra

networks:
  coolify-infra:
    external: true
